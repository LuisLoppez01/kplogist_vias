@extends('adminlte::page')@section('title', 'FFCC')@section('content_header')    <h1>Empresas</h1>@stop@section('content')    @if (session('info'))        <div class="alert alert-success" role="alert">            <strong>Éxito!</strong> {{session('info')}}        </div>    @endif    @if(session('error'))        <div class="alert alert-danger">            <strong>Error!</strong> {{ session('error') }}        </div>    @endif    <div class="card">        <div class="card-header">            <a href="{{route('menu.companies.create')}}" class="btn btn-primary">Registar empresa</a>        </div>        <div class="card-body table-responsive">            <table class="table table-striped">                <thead>                <tr>                    <th>ID</th>                    <th>Nombre</th>                    {{--<th>RFC</th>--}}                    <th>Ubicación</th>                    <th colspan="2"></th>                </tr>                </thead>                <tbody>                @forelse ($companies as $company)                    <tr>                        <td>{{$company->id}}</td>                        <td>{{$company->name}}</td>                        {{--<td>{{$company->RFC}}</td>--}}                        <td>{{$company->location->name}}</td>                        <td width='10px'><a class="btn btn-secondary" href={{route('menu.companies.edit',$company)}}>Editar</a>                        </td>                        <td width='10px'>                            @can('delete', $company)                                <button                                    onclick="Livewire.emit('confirmDeletion', '{{ route('menu.companies.destroy', $company) }}')"                                    class="btn btn-danger">                                    Eliminar                                </button>                            @endcan                        </td>                    </tr>                @empty                    <tr>                        <td colspan="3">No hay ninguna empresa registrada</td>                    </tr>                @endforelse                </tbody>            </table>            <div class="mt-4">                {{$companies->links()}}            </div>            @livewire('delete-confirmation-modal')        </div>    </div>@stop@section('css')    <link rel="stylesheet" href="/css/admin_custom.css">@stop@section('js')    <script> console.log('Hi!'); </script>@stop